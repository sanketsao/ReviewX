{
  "name": "@reviewsx/e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end + channel tests for ReviewSX (overlay, inbox, published listings).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
