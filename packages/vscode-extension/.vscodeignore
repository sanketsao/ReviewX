# Everything the bundled extension doesn't need at runtime.
# out/extension.js is the esbuild bundle; dist/ has the overlay bundles.

src/**
node_modules/**
.vscode/**
tsconfig.json
build-vsix.mjs
.gitignore
.vscodeignore
**/*.ts
**/*.map
*.vsix
